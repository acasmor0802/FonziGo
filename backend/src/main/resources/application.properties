spring.application.name=backend

# Server
server.port=8080

# Database (PostgreSQL)
spring.datasource.url=jdbc:postgresql://database:5432/fonzigo
spring.datasource.username=fonzi
spring.datasource.password=fonzipass
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# JWT Configuration
security.jwt.secret-key=tu_clave_secreta_super_larga_con_minimo_veinticinco_caracteres_para_HS256_JWT_TOKEN
security.jwt.expiration-time=86400000

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=INFO
